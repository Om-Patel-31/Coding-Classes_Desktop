#include <stdio.h>
#include <conio.h>

int main() {
    char ch;
    // char name[100];
    printf("\nEnter a character: ");
    // ch = getchar();
    ch = getch();
    printf("\nYou entered: ");
    putch(ch);
    // printf("%c\n", ch);
    // putchar(ch);
    // printf("Enter your name: ");
    // gets(name);
    // printf("Your text is: ");
    // puts(name);
    return 0;
}